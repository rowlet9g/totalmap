{
  "name": "totalmap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "node scripts/preview.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=22" }
}
